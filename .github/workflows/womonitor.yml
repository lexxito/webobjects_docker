name: womonitor workflow

on:
  push:
    branches: [ classical_webobjects_ant_scalable ]
  workflow_dispatch:
    branches: [ classical_webobjects_ant_scalable ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: docker login
      env:
        DOCKER_USER: ${{secrets.DOCKER_USER}}
        DOCKER_PASSWORD: ${{secrets.DOCKER_PASSWORD}}
      run: |
        docker login -u $DOCKER_USER -p $DOCKER_PASSWORD 
    - name: Build womonitor Docker image
      run: docker build . --file deploy/dockerfiles/womonitor/Dockerfile --tag lexxito/womonitor:7.4-jdk11
      
    - name: Docker Push womonitor image
      run: docker push lexxito/womonitor:7.4-jdk11
